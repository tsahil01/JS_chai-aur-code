const arr = [1 , 4, 5, 7, 8, 7]

// arr.forEach((item, index, ar) => {console.log(item, index, ar);})

const obj = {
    name : "Sah",
    class : 10
}

for (const key in obj) {
    console.log(key);
}