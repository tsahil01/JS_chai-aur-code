// if =>

// if(condition){
//     // scope
// } else {

// }

// <, >, <=, >=, ==, ===, !=, !==

// Terniary Operator -->
// condition ? true : false

const iceTea = 100
iceTea > 80 ? console.log("greater than 80") : console.log("More than 80");